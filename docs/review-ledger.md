# Review Ledger

Every review-pipeline run logs one line. Pipeline spec maintained privately.

Conventions (2026-07-16): the Findings column breaks confirmed findings down
by stage (`SDE1:n SDE2:n SDE3:n SSE:n BRK:n`, suffixed B/H/M/L for severity) —
a stage at zero confirmed findings across 5 consecutive FULL runs gets merged
or cut. Calibration drills (seeded-defect recall runs on package copies; seeds
never committed) log here with `CAL` in the Stages column and recall in the
Verdict column.

| Date | Change | Path | Stages | Findings | Verdict |
|------|--------|------|--------|----------|---------|
| 2026-07-14 | scaffold: repo bootstrap (f512c6f) | LIGHT | SDE-1 + CTO gate | 1 (cmd/ stubs missing main — fixed pre-merge) | PASS — evidence: go build/vet/gofmt clean, ignore rules verified, no product code in internal/ |
| 2026-07-14 | review: scaffold, 5-lens + CTO gate | FULL | secrets/workflow/config/ip/breaker + CTO verify | 9 confirmed, 0 refuted (2 HIGH: make-lint swallowed failures; kvstash namespace unclaimed) | ACTIONS APPLIED — Makefile lint fixed, NOTICE holder corrected, README claims hedged, ledger hash corrected, CI hardened (persist-credentials off, dependabot added); org registration pending user |
| 2026-07-15 | xferspike A1 transport rig (frame.go/main.go/spike.go) | FULL | 4 lenses + 3 breakers + CTO gate | 1 HIGH + 3 MED + 8 LOW; blocker CONFIRMED then FIXED (stalled-peer hang, no write deadline) | FIX-FIRST → applied: write deadline (proven), unit relabels (gbytes_per_s/gbit_per_s, cpu_cores_sender), byte-order lock, validations; 3 LOW deferred to the cloud rig |
| 2026-07-15 | fix gosec G115 (CI-caught) | LIGHT | golangci-lint local repro + CTO self-check | 2 (int->uint32 truncation) | PASS — real MaxUint32 bound added in runClient (kills latent truncation); drill uses typed const; nolint w/ justification. golangci-lint: 0 issues |
| 2026-07-15 | soak rig (soak.go, arena_*.go) + aws-transport scripts | FULL + shell breaker | 4 lenses + 2 breakers + CTO gate | 3 HIGH + 4 MED + 4 LOW; core A2 mechanism verified honest | FIX-FIRST → applied: reporting-honesty (valid flag + omitempty percentiles, no false-PASS), munmap-wait (use-after-free), teardown all-region sweep, provision security group, first unit tests; verified degenerate run refuses percentile |
| 2026-07-15 | free gates A5/A6/A7 (economics.py, check_imports.py, vllm-matrix.yml, a5/DESIGN docs) | LIGHT + 2 breakers | econ-review + tripwire-review | 1 MED + 1 HIGH + minors | FIXED: A7 verdict was rigged always-PASS -> real 2-part gate; tripwire false-green on renamed class/module -> two-stage import (DRIFT vs SKIP); dropped dead auto-issue job (F2/F3/F4); CPU-torch nudge. Verified: renamed class=exit1, absent=exit0 |
| 2026-07-15 | whole-codebase holistic review (5 review agents) | FULL cross-file | sde3-systems + correctness + shell-ci-python + security + CTO | 0 CRIT/HIGH — codebase SOUND; 4 MED + several LOW | FIXED: DESIGN MLA break-even (reintroduced miscount), economics decimal-GB base (7% skew), dead versions.env removed, provision ERR trap, check_imports abstractmethods false-RED->WARN, Makefile bench, CI tool pins, A7 cost-arm honesty; ADDED loopback -race test. Deferred (benign): WaitGroup lifecycle refactor |
| 2026-07-15 | nvmeprobe A3 rig + aws-nvme scripts | LIGHT (follows reviewed rig patterns; full ladder deferred to pre-A3-run) | self-check: dual-platform build, lint 0 issues, Mac smoke 1.75 GB/s | giouring dead on Go 1.26 (linkname) -> pre-registered threadpool fallback taken, uring deferred to v1.1 | committed; A3 gate rule: threadpool >=6 GB/s/device = PASS |
| 2026-07-16 | quality upgrades: mutation gate (make mutate), ruleguard rules, ledger metrics | LIGHT | SDE-1-equivalent self-check + machine evidence | mutation run exposed 4 real test gaps (unpinned limits/feature-bits/sub-op placement; zero-key golden blind to offset shifts) — all closed; MSI 0.53→0.9775 (4 survivors provably equivalent); ruleguard first run correctly flagged the 2 documented BE exceptions | PASS — lint 0 issues, -race green, mutate gate green at 0.90 floor |
| 2026-07-16 | CALIBRATION DRILL #1: 6 hand-seeded bugs (real-pattern: validation-order, self-consistent endianness pair, error-class downgrade, constant typo, field-width, test-coverage cut) in a blind standalone copy of the header codec; suite kept green over all seeds | CAL (blind SDE-2 + SSE + breaker) | per-lens recall: SDE2 6/6, SSE 6/6, BRK 6/6 (union 6/6); 0 false positives; all 3 independently detected the weakened-test meta-pattern; breaker produced runnable repros + a control case | **recall 100% (6/6)** — difficulty tier 1 (code contradicted doc comments); next drill: seeds whose comments agree with the code, and concurrency-class seeds once transport lands |
| 2026-07-16 | PROTOCOL.md v1 freeze + internal/protocol core (header/ops/crc + tests) + DESIGN A1/A2/A3 | FULL | SDE-1 + SDE-2 + SDE-3 + SSE + 2 breakers (parser-fuzz, spec-lawyer) + CTO gate | 2 BLOCKER (spec: ABORT response contradiction, HELLO padding ambiguity) + 6 HIGH (5 spec interop: status-array padding, non-OK body shape, FATAL frame form, credit grant-back leak, HELLO request_id; 1 process: A3 "PASS" renegotiated a pre-registered gate) + 6 MED + ~20 LOW/clarifiers; code survived 19M+21.9M fuzz execs with 0 crashers | FIX-FIRST → all applied: spec amended pre-freeze-commit, A3 recorded NOT-MET-as-written w/ owner disposition pending, Magic var→const, over-cap returns populated header, HeaderCRC len-contract enforced, golden vectors (real CRCs) + FuzzParseHeader landed, cadence leaks stripped. Evidence: build/vet/lint 0 issues, -race green, 60s fuzz PASS |
| 2026-07-16 | internal/protocol batch codecs + internal/config + internal/transport (conn/credit/listener/bufsrc) + fuzz CI | FULL (Ladder v2: blind authorship, HIGH+ refute-or-promote, per-stage yield, cross-model breaker) | SDE-1×2 + SDE-2×2 + SDE-3×2 + SSE×2 + holistic×2 + parser-breaker + batch-breaker + Opus-4.8 diversity-breaker + CTO gate | per-stage yield SDE1:0H SDE2:1B/1H SDE3:3H SSE:1B holistic:1H BRK:1H; 1 BLOCKER (fuzz.yml invalid YAML) + ~7 HIGH (teardown-lattice stranded-release ×convergent-6, iovs realloc, credit-strike poisoning, NOP answered, slow-loris body buffer, empty-config crash, NegotiateLimits blob>frame) + MED/LOW; codec held 44.7M+31.5M fuzz execs, 0 crashers | FIX-FIRST → all confirmed applied: fuzz.yml valid+re-fails on crash; writer-death Close + post-wg.Wait finalDrain + bounded teardown; iovs copy-to-WriteTo; Account() strikes-free over-cap path; NOP routed first; body-read deadline; io.EOF=defaults; blob clamped under frame; Listener.logged atomic; stall-timeout floor; kvbdebug credit-conservation assert built; go.mod tidied + CI check. Cross-model breaker findings all confirmed by convergence. Day-5 seams recorded (BufferSource frame ctx, RemoteAddr, config→transport bridge, pre-auth-fatal). Evidence: lint 0, -race + kvbdebug green, mutation 0.928, fuzz clean, both workflows valid |
| 2026-07-16 | internal/server + internal/store/ramstub + pkg/client + cmd/kvblockd (wire path end-to-end) + throughput-gate resolution | FULL (Ladder v2: blind authorship, HIGH+ refute-or-promote) | SDE-2 + SDE-3 + SSE + holistic + refuters + CTO gate | ~5 HIGH (client non-OK GET read-ahead deadlock; pool never heals desynced conns ×convergent-3; unbounded PUT staging via cheap BEGINs — 4 KiB pinned ~191 MiB; BATCH_GET emitted frames over the negotiated cap, no F_MORE split; zero-length CHUNK reset the reaper timer) + MED (BEGIN-on-tombstone blocked id reuse, client advertised FEAT_OOO it lacks, HELLO reject rode the generic fatal report, Drain accept TOCTOU, tombstone map growth) + LOW | FIX-FIRST → all applied + regression tests landed (F_MORE round-trip, non-OK no-hang, ABORT matrix, overflow-tombstone, 256-live-stream cap, pinned-clock reaper sweep, write-once matrix, pool-heal + no-hang via scripted fake server). Gate resolved by measurement: cold GET 9.5–9.7 GB/s peak (was 7.4) = parity with the raw-socket same-shape ceiling (bench/microbench/rawget, interleaved runs; A1 %-of-ceiling rule) — the 14.1 xferspike figure is a one-way hot-buffer blast, a different shape; delta + methodology recorded in DESIGN.md, Linux re-run is the Week-3 item. Evidence: lint 0 issues, vet, -race all pkgs, kvbdebug green, mutation 0.9276 ≥ 0.90, fuzz 60 s × 2 clean |
| 2026-07-16 | overnight throughput push: decomposition + pipelining matrix + research pass 2 | BENCH+RESEARCH (no ladder needed: bench/docs + one tripwire; client SkipVerify covered by regression test) | interleaved A/B vs raw-socket ceiling, two-process production shape, Linux-VM cross-check, 103-agent adversarially-verified research | verification-cost decomposition: wire path 11.27 GB/s = 102% of raw ceiling (11.04), full-integrity default 9.75 = 88% — remaining delta is xxh3, now a documented dial; pipelining measured FLAT on loopback (µs bubbles), recorded as network-rig lever; Linux 6.10 VM ratio 92%; research verified: goroutine-per-conn+writev correct at ≥1 MiB (ByteDance's own guidance), #21676 writev fast-path pitfall (tripwire added), MSG_ZEROCOPY zero on loopback / max-benefit at 1 MiB on NICs, memory BW is terminal ceiling | wire path CLOSED at >100% of same-shape ceiling on 2 kernels; gate quoted %-of-ceiling per A1 rule; formal 1h fuzz gates running; remaining levers all live on real-NIC rigs (Week 3/7) |
| 2026-07-17 | perf pass 2: no-copy PUT, incremental digest, buffer recycling, Delete verb, verb/codec benchmarks | FULL (Ladder v2 workflow: 6 blind lenses — client-scratch/server-lend/concurrency/dos/spec/test-adequacy — + independent refuter per HIGH) | blind panel over the diff-as-external-contribution + Fable-5 refuters; interleaved A/B benches; research pass A/B (104 agents) informed the levers | 3 HIGH confirmed 0 refuted (tombstone retained 1.2KB Hasher → per-tombstone DoS amplification; grow-only lendBuf pinned up to credit window per conn; Delete verb zero test coverage) + 14 MED/LOW; concurrency + single-live-Lend lenses returned clean (verified, not assumed) | ALL 3 HIGH fixed + high-value MEDs (append-slack via first-chunk exact-cap alloc, zero-key GET hang guard, Delete count cross-check) + regression tests (Delete e2e/slot-release, multi-chunk digest, zero-len commit); measured PUT 1-RTT −20%, EXISTS depth-16 5.1×, PUT allocs 3.1MB→1.05MB; -race + kvbdebug green, lint 0 |
| 2026-07-17 | perf pass 3 (research-confirmed): descScratch reuse, CI flake fix, doc honesty corrections | RESEARCH+LIGHT (bench/docs/test-only + 1 safe hot-path scratch reuse) | 3rd deep-research pass (107 adversarial agents) + interleaved re-measurement + 50x local flake stress | research CONFIRMED design (no new lever): keep xxh3 over CRC32C (faster + collision resistance), per-conn scratch GC-safe under Green Tea (noscan), alloc-elimination is GC-hygiene not throughput (memory-BW bound). Self-corrections: cherry-picked 11.27 headline -> reproducible ~0.97 ratio; inline-vs-sidecar "6.9 vs 9.75" was a thermal confound -> near-equal interleaved. Fixed pre-existing CI flake (TestFatalBadCRCReportsAndCloses: shared 10s dial deadline vs best-effort report under CI oversubscription; 0/50 local). | descScratch -1 alloc (throughput-neutral, labeled); flake deflaked (strict assertions kept); docs corrected; -race + kvbdebug + lint green; CI green |
| 2026-07-17 | apply remaining panel MED/LOW findings | LIGHT (client robustness + test additions + doc/comment) | full pre-push gate (build/vet/lint/-race/kvbdebug/mutation/fuzz-smoke) required green before push | client readN scratch bounded at 1 MiB (malicious-server pin class, matches lendBuf fix); Delete rejects non-OK (OK_EXISTS masking); interleaved-stream digest-isolation test; scratch-aliasing copy-out contract test; EXISTS RTT comment corrected; append-slack findings confirmed already-resolved by exact-cap staging | all applied; gate green (lint 0, -race + kvbdebug all pkgs, mutation 0.9276, fuzz smoke clean); pushed |
| 2026-07-17 | per-benchmark optimizer fan-out (32 Fable-5 agents, 7 angles x 4 benches) → test + apply winners | BENCH-ITERATION + full pre-push gate each commit | A/B measured each candidate; kept only measured/safe wins, discarded losers honestly | 72 candidates recovered from journal (ranker crashed on transient API errors); WINNERS: ExistsPrefix no-bitmap early-exit (30ns vs 723ns = 24x on prefix-miss), single-chunk one-shot PUT digest (skip ~1.2KB Hasher; PUT 32→31 allocs), verifyErr alloc only-when-verify, precomputed immutable status-ack preambles (PUT 31→29 allocs, zero-alloc acks); consistency: per-session key-status scratch. REJECTED: codec slices.Grow in-place descriptor writes (34ns vs 29ns baseline — slower); codec sub-ns micro-opts (thermal-noise-swamped). Verification-correctness (self-check): caught own flawed grep-based gate reporting phantom "4 failures" → re-verified via exit code (kvbdebug/race exit 0). | PUT 32→29 allocs/op; ExistsPrefix 24x on no-bitmap miss; all gates green each push |
| 2026-07-17 | GET response region sync.Pool (via WriteFrames release) — BUILT, REVIEWED, REJECTED | FULL (blind 4-lens async panel + refuters on the diff) | race + kvbdebug (poison + credit-conservation) clean; blind panel traced the release contract | 0 BLOCKER/HIGH; decisive LOW: pooling the region in the release callback is safe ONLY under synchronous writev — release is the documented MSG_ZEROCOPY errqueue seam, and a future zero-copy writer would fire release while the kernel still references the region → Put → next Get reuses → cross-frame corruption. Saves 1 GET alloc (49→48), throughput-neutral. | REVERTED (not committed): a 1-alloc throughput-neutral win is not worth baking a corruption landmine into the roadmapped zero-copy path. Do NOT re-attempt region/bufs pooling via release until the write path is zero-copy-final. |
| 2026-07-17 | per-benchmark fan-out round 2 (52 agents, 13 angles) → test candidates | BENCH-ITERATION + blind panel on the risky one | A/B measured; blind panel on async change; honesty-check on a flagged methodology concern | 106 candidates. OUTCOMES: (1) GET region sync.Pool — built+tested+panel-reviewed → REJECTED (zero-copy release landmine, see prior line). (2) LLC-residency honesty concern (getbench re-reads a hot set?) — investigated via new getbench -pool flag: at FIXED batch=32, GET is working-set-invariant 32MiB→1GiB (7.1/7.6/8.3/5.5) = NOT cache-optimistic, headline stands. (3) writeVector oversized-iovec split (target 2.5MB dip) — built+A/B → NOT A WIN (streams=4 identical 7.30 vs 7.34 old) → REVERTED. (4) getbench stale-daemon size guard — WIN, refuses the write-once-reuse inflation artifact (once produced a bogus 2.5x reading). | 0 code opts shipped (2 attempted, both rejected on evidence); 2 harness/honesty improvements shipped (-pool flag, stale-daemon guard); GET cache-optimism disproven. Nothing worse on main. |
| 2026-07-17 | AWS A1 real-NIC gate: c6in.8xlarge pair, 50 GbE (Rudray-authorized, $20 cap) | RIG-RUN (proven scripts) + hard 75-min teardown watchdog + $0-residue assert | iperf3 ceiling denominator; xferspike proxy + kvblockd end-to-end over the private link; verify ON vs OFF | iperf3 49.8 Gbit/s; xferspike 6.27 GB/s = 100% ceiling @ 0.79 cores; kvblockd GET 6.37 GB/s = 51.0 Gbit/s = ~102% ceiling (verify ON), scales 8→64 streams; verify ON==OFF (xxh3 FREE on a real NIC — the loopback verify-gap was a loopback artifact) | A1 PASS (≥12 loopback [14.1] AND ≥85% ceiling [~100%]); kvblockd is NIC-bound end-to-end; ~$2 spend, $0 residue verified across 3 regions |
| 2026-07-17 | 100 GbE headline run: c7gn.8xlarge Graviton pair (Rudray-authorized, quota-aware substitution for c6in.16xl) | RIG-RUN + watchdog + $0-residue assert | iperf3 ceiling 99.8 Gbit/s; kvblockd arm64 end-to-end sweep 16→96 streams; verify ON vs OFF | **kvblockd GET 12.67 GB/s = 101.4 Gbit/s = ~102% of the 100 GbE ceiling, verify ON — the project's 10+ GB/s target MEASURED**; verify ON==OFF replicates; Graviton saturates at half x86 vCPU/price; scripts gained ARCH param (arm64 AMI) | ~$2 spend (node A spot), $0 residue verified; a1-log + BENCHMARKS updated |
| 2026-07-17 | W3D1-3: OffsetAllocator port + mmap arena + kvbctl | FULL Ladder v2 (blind: 6 Fable-5 lenses + Opus-4.8 diversity breaker + refuter per HIGH; refuters wrote REPRODUCING tests) | falsifier-first build (SmallFloat exhaustive vs brute force before structure; rapid 10k-check deep run clean first try — pre-registered fallback never triggered); per-stage yield: sde2-alloc 3/0H, smallfloat 1/0H, arena-unsafe 2/0H, concurrency 2/1H, sse 3/0H, test-adequacy 6/1H, breaker 2/0H | 2 HIGH confirmed 0 refuted, both empirically reproduced: (1) Bytes/Close TOCTOU — plain base field let a racing Bytes fabricate a wild slice via unsafe.Add(nil,off) (refuter produced the -race report) → base now atomic.Pointer loaded once; (2) stale-Free ABA — slot recycling let a stale handle silently free a LIVE allocation in BOTH build modes (refuter reproduced exact accounting corruption) → 14-bit generation counter packed into Meta. MEDs fixed: failed-Alloc zero-value aliased first live handle → sentinel; pool-size knob (NewAllocatorMax); StorageReport pool-exhaustion guard restored; kvbctl put status→exit 1; get -o close error; rapid pairwise disjointness (tag wrap blind spot); non-bin-exact capacity test; fragmented churn benchmark; ruleguard unsafe fence extended to Add/Slice/SliceData/String/StringData; advisePopulate ENOMEM≠EINVAL; tautological unit test → real units round-trip via arena | ALL confirmed findings fixed + regression-pinned (tagged release/kvbdebug siblings); gates: lint 0, race+kvbdebug exit 0 on darwin AND Linux(Docker), deep rapid 5k re-run clean post-fix, GC-invisibility (negative heap delta on Linux), real hugepages verified (privileged Docker), alloc bench ~22ns/0 allocs (target <200ns), kvbctl e2e byte-identical |
| 2026-07-17 | DRAM tier + server wiring + metrics + measured gates (5 commits, pushed) | FULL Ladder v2 (blind: SDE-1/2/3 + SSE on Fable 5 + Opus-4.8 diversity breaker read-only; refute-or-promote — 4 refuter agents, all 4 CONFIRMED; 2 HIGHs confirmed by direct quoted-text inspection, 1 BLOCKER by an independent stage's empirical reproducer) | per-stage yield: sde1 2H/11ML, sde2 1B/2H/3ML, sde3 1H/4M/2L (also verified the release-exactly-once/allocMu/frame-split crux clean), sse 2H/3M/2L, breaker 2H/4M (both HIGHs confirmed by Fable-5 refuters — the diversity thesis paid); race+kvbdebug on darwin AND Linux(Docker) clean post-fix, lint 0, 5-count dram stability clean, per-commit standalone builds verified | BLOCKER: spec-legal empty block → ERR_QUOTA_BYTES (fixed: extent-less blocks; conformance case added both stores). HIGHs fixed: soft-pin quota debit vs §3.6 (charge only into-hard, upgrade gated, downgrade refunds — wire test rewritten from asserting the bug); ERR_QUOTA_BYTES on COMMIT outside the frozen §3.4 set (advisory BEGIN quota check + commit race → ERR_BUSY; spec amended 1 line); drain-timeout munmap-under-writev chain incl. the flushBy deadline re-arm erasing Close's poke (post-close writes now cap at the 1s drain budget; Drain returns success; main skips unmap on timeout; accept-race conn drained via acceptDone); CanEvict structurally false for every resident block (refcount==1 pre-filter, race-audited by refuter); main.go defer-order startup deadlock; 2 lying handler doc comments. MED/LOW fixed: BEGIN ttl_ms dropped, empty BATCH_GET silent, blob==frame off-by-header (negotiation headroom), plain Get view-after-release (copies now), writeLoop fd leak (refuter bonus find), ns echo, pprof non-loopback warning, CHUNK frames polluting op histograms, pool-cap honesty docs, 8 comment/test hygiene items. My own fix pass introduced 2 regressions the suite caught (over-aggressive close poke killed the §9 fatal-report drain; a test pinned the old blob clamp) — fixed, both legs green. | Gates on Linux (c7i.4xlarge, aws-dramgate rig): G1 0.96-0.97× rawget same-shape ceiling (26.2-26.4 GB/s absolute); G2 EXISTS p99 705µs @ 1.2M samples under 8 saturated GET lanes; G3 zero per-request blob-band allocs (largest 896B). Total AWS ~$1.5, $0 residue verified. Pushed c7d4272; Merge Rule: Rudray must explain back the refcount/release contract, the pin-quota transition rules, and the drain-before-unmap chain before Week 4 builds on them. |
| 2026-07-17 | Eviction subsystem + model-based harness (7 commits, pushed b7677ab) | FULL Ladder v2 (blind: SDE-1/2/3 + SSE on Fable 5 + Opus-4.8 breaker read-only; refute-or-promote: 1 measuring refuter for the breaker's 2 findings, empirical reproducers for the rest) | per-stage yield: sde1 3H/10ML, sde2 3H/3M/3L (incl. the lease-truncation HIGH the harness was structurally blind to), sde3 1 REPRODUCED BLOCKER/1H/1M/3L (+ verified the whole lock story clean), sse 1H/4M/4L (mutation audit found 2 harness holes), breaker 1H/3M/4L; refuter: convoy REFUTED at HIGH by measurement (insert p99 ≈ eviction-free contention; killing the sync pass costs 47× goodput; recorded as a p999-tail note w/ revisit gate), zero-len DoS CONFIRMED by measurement (181 B/block unbounded → nominal count slots + count-goal emergency sweep + regression test); the refuter independently RE-REPRODUCED the BLOCKER on its snapshot (3rd confirmation) | BLOCKER fixed: S3-FIFO Admit/Remove atomicity gap (zero-value where == qSmall → FIFO corruption → evictor panic; qUnqueued + tombstone handshake + 5-goroutine hammer regression). HIGHs fixed: GET auto-lease truncated longer leases (monotonic extendLease; spec = grant/extend); evictOne conflated gone/refused → permanent policy phantoms (3-way outcome + regression); soak driver exited 0 on client checksum failures (checksum → verifyFails, exit 1 on any hard error); policy.Remove raced re-publish (moved inside the DeleteIf gate). Harness holes closed: maybeGone now clears on every authoritative presence proof; held refs track their block GENERATION; I3 bidirectional pinned + live_allocs ≤ blocks+held. THE DEEP GATE CAUGHT TWO ORACLE BUGS OF OURS pre-verdict (extent-leak bound missing the held-ref allowance; the model not mirroring the monotonic-lease fix — the store was right, the model wrong) — the oracle audits itself. Learned: rapid .fail seeds are non-replayable here (policy seeds + map iteration) and rapid hard-fails on stale seeds → regressions live as explicit tests, seeds are CI triage artifacts. | GATES: 10⁶-op rapid gate PASS (20000 checks × 500 steps, 62 min, race+kvbdebug, both policies drawn, zero violations); stress: 50k byte-verified GETs vs 18k live evictions clean; live-daemon smoke 120 MiB through 64 MiB arena, 0 failures; A/B policy-vs-none = parity (s3fifo won 2/3 interleaved pairs); lint 0; darwin+Docker legs green; per-commit standalone builds. 24h AWS soak launching (m7g.xlarge, 27h dead-man). Merge Rule: Rudray owns the eviction walk, ghost-hash rationale, lossy-store-strict-oracle asymmetry, refcount happens-before, gctrace reading. |
| 2026-07-17 | LMCache adapter: Python wire client + RemoteConnector + Go↔Python hash parity + CPU e2e/CI (5 commits) | FULL Ladder v2 (blind: SDE-1/2/3 + SSE on Fable 5 + Opus-4.8 breaker; refute-or-promote via reproducers + cross-stage agreement + source verification of the 2 at-risk upstream APIs) | per-stage: sde1 2H/7M/5L, sde2 3H(1 BLOCKER-rated)/3M/4L, sde3 2 REPRODUCED BLOCKERS/3H/3M/3L, sse 1 BLOCKER/3H/6M/2L, breaker 3H/4M/3L. Two BLOCKERs reproduced by SDE-3 (/tmp reproducers) AND independently re-found by Opus+SSE: (1) tile-index collision — GET >max_batch_keys reused tile-LOCAL alloc indices → cross-key KV corruption fed to the model + MemoryObj leak; (2) Pool.run non-exhaustive except → semaphore+socket leak → permanent pool hang. Status-enum divergence rated BLOCKER by SSE (my from-memory codes: 0x20-0x23/0x50 wrong, EVICTED/ERR_INTERNAL/FATAL_PROTOCOL missing → ValueError on a legal per-key byte → feeds the pool leak). No separate refuter agent needed: all HIGH+ were reproduced, ≥2-stage-confirmed, or source-verified (WebFetch'd LMCache 0.5.1 memory_management.py to confirm allocate(shape,dtype,fmt=...) and MemoryObjMetadata.fmt). | FIXED: tile-index (idx_base threads the GLOBAL index; 600-key regression); Pool.run (except BaseException→discard+release; slot-survival regression); Status enum rewritten verbatim from ops.go + tolerant to_status (regression); asyncio-cancel use-after-release (ref_count_down moved INTO the executor thread + asyncio.shield on put; cancelled get ref-downs its abandoned prefix via done-callback); mem_fmt round-trip (threaded into allocate; monkeypatched-enum regression); non-contiguous alloc → copy-then-zeros-with-valid-xxh3 (refuse-not-copy tripwire); non-OK-non-NOT_FOUND descs skipped like Go readGetInto; F_MORE window + count bounds validated; corrupt embedded golden fallbacks regenerated from the real hex (+ always-run fallback test); _ensure double-dial lock + closed flag; exists_sync routed off the event loop; _dial_one fd leak; Header wrong-length-key now raises; ci.yml connector job (pytest off the pytorch index; --no-deps); numpy dep; go.mod blake3 // indirect (go mod tidy); .gitignore python caches; INTEGRATIONS matrix/pin coherence + PyPI wording; verify.py honesty + e2e restart step for property (d); adversarial hash vectors (empty/unicode/boundary, 56 total). | GATES: 24 kvblockd + 12 connector Python tests green (incl. 25× repeat-soak, tile + pool + status + mem_fmt regressions); Go↔Python parity 56 vectors both languages; ruff 0; go build/vet/tidy clean; Docker-Linux Go green. Mac e2e: no macOS vLLM wheel exists (PyPI linux/sdist only) → Gate-1 is CI-only, Mac runs layers 1-4 natively (recorded). Merge Rule: Rudray owns the key path end-to-end, the never-raise/never-hang contract, the tile-index-collision fix, the zero-copy read path, batched_contains consecutive-prefix semantics. GTM (Slack post, docs-listing, PyPI publish) prepared for Rudray to fire post repo-public flip. |
| 2026-07-18 | NVMe tier: log-structured segments + tiered orchestrator + crash recovery + torture harness (staged commits) | FULL Ladder v2 (blind: SDE-1/2/3 + SSE on Fable 5 + Opus-4.8 breaker read-only; refute-or-promote satisfied by cross-stage agreement + THREE independent empirical reproductions of the blocker cluster — SDE-2, SDE-3, and SSE each built live repros; every other HIGH verified against source before fixing) | per-stage yield: sde1 2H/4M/7L, sde2 2B/2H/4M/4L, sde3 1B/2H/3M/4L (+ verified lock order, read acquire/release pairing, bufPool balance, fsync story clean), sse 1B/1H/7M/6L (+ verified ERR_BUSY-descriptor spec-legality, client tolerance both languages, goldens untouched, DRAM-only scrape purity), breaker 2B/2L — the diversity seat found the blocker FIRST; the checkpoint-trust cluster was then independently confirmed by three Fable-5 stages. | BLOCKER CLUSTER fixed (recovery.go/checkpoint.go): (B1) ckpt-trusted segments carried entries=nil → the NEXT checkpoint erased them (2nd-restart silent loss of sealed+fsync'd data, reproduced 3×: 24/40, 12/15, 8/12 blocks lost) + reclaim gated on an empty table (leased/pinned blocks retired ungated; permanent EXISTS-yes/GET-no phantoms) → trusted segments now carry ckpt-derived entry tables with per-file bounds; (B1b) ckpt skipped dying segments from entries but not maxSealed → aborted retire + crash lost the whole segment (reproduced) → maxSealed now capped below any dying id (or the round skipped). HIGHs fixed: same-segment same-key merge kept the OLDER offset (served deleted content; reproduced) → (segID, offset)-lexicographic latest-wins; segment-size retune DELETED every sealed segment → per-file geometry (only genuinely partial creates dropped); write-once TOCTOU across the demotion publish window (PUT of different bytes acked OK) → post-insert recheck withdraws + ERR_IMMUTABLE_CONFLICT; NVMe lease grants raced reclaim's gate (TOUCH_LEASE OK on a removed entry) → all nvme lifecycle mutations under the shard lock, nil→NOT_FOUND; PromoteWindow "0 = never" was silently 60s → 0 genuinely disables (main.go workaround removed); modeltest crash model contradicted documented delete-resurrection semantics (guaranteed-eventual CI failure, reproduced) → epoch-stamped ghost history model (resurrection accepts only historically-committed bytes — I1 preserved). MEDs fixed: PinFlags pre-gate data race (shard-locked); writer abandoned queued appends on Close (arena-ref leak, DemoteNow hang) → drain-with-callbacks both sides; segment-ID reuse vs stale ckpt (nextID clears ckpt maxSealed); non-canonical seg filenames double-counted used + leaked fds (canonical-name filter + regression); nvme_paths aliasing/nesting (normalized + prefix-rejected); unbounded ckpt read (512 MiB cap pre-alloc); write-dead volume after failed rotation (nil-seal no-op + TryRecoverWrites on the reclaim tick); heap buffers on O_DIRECT metadata paths (alignedTemp mmap buffers); config/volume min-segment formula gap (shared nvme.MinSegmentBytes); Read-vs-Close hang/panic (readStop protocol, workers drain with ReadGone); torture oracle gained EXISTS-first phantom detection (the GET-only oracle passed the blocker vacuously — SDE-2's find); Stats reclaims_total key shadowing; frame-boundary re-fetch self-triggered promotion (10ms min-gap); ckpt .tmp orphans pruned; 0600 file modes; false nolint + 6 lying comments + DESIGN/doc.go overclaims corrected; PROTOCOL.md §3 gained the (always-legal) per-key ERR_BUSY clarification note. ACCEPTED/DEFERRED with rationale: PIN_HARD answers ERR_BUSY when promotion can't land (stricter-than-spec DRAM residency, retryable — documented); python client maps ERR_BUSY→miss (no desync; retry signal invisible — client follow-up); volumeFor is positional (documented: nvme_paths is append-only); I3 dual-residency bound stays 2× (phantom class now covered by the torture oracle + scrub). | GATES post-fix: full repo -race -short green; kvbdebug leg green; rapid 1000 checks (two-tier + I6 + resurrection) green in 54s; 6 new ladder-regression tests pin B1/B1b/same-segment/geometry/junk-names/close-callbacks; lint 0 across ./...; fresh 100-loop Docker/Linux torture with the phantom-detecting oracle: PASS. Merge Rule: Rudray owns the recovery walk (ckpt trust boundary → footer scan → tail truncation), the durability contract (what a COMMIT ack promises; why the journal writes after the ack; why a torn tail can't hurt sealed segments), O_DIRECT alignment discipline, and the delete-resurrection semantics before Week 7 builds on this. |
| 2026-07-18 | kvbench harness: sweep/replay/fill/verify/convert/report + CO-safe open-loop scheduler + kvops converters + baselines/traces/charts/docs (staged commits) | FULL Ladder v2 (blind: 2 Fable-5 lenses [correctness, style/spec] + Opus-4.8 diversity breaker read-only; refute-or-promote satisfied by cross-stage agreement — 3 core findings independently cross-confirmed; all HIGH+ verified against source before fixing) | per-stage yield: breaker 3H/2M (found the RunOpen inflation FIRST — the diversity seat paid again), correctness 2B/3H/5M (confirmed breaker's #1 by independent derivation, cleared 4 worried-about constructs incl. CO-test non-vacuity + go-redis aliasing), style/spec 0B/0H/6M/8L (doc/script overclaim sweep) | totals 2 BLOCKER / 6 HIGH (3 cross-confirmed) / 11 MED / 8 LOW — a benchmark that lies is worse than none, and every number-affecting bug was caught pre-rig. BLOCKERs fixed: (B1) sweep re-PUT the same keys across closed+6 open runs — on write-once stores every later PUT was a no-op ack measured as a real write, biasing the cross-store PUT bars toward kvblockd → atomic putCtr issues unique PUT keys per cell run (+regression); (B2) OK_EXISTS conflated with OK in every driver so no-op PUTs credited full payload bytes → Status gains Exists + Wrote(), only written bytes count (+regression). HIGHs fixed: RunOpen had no wall-clock cutoff — at overload it drained the whole precomputed backlog but divided by the nominal window, so saturated throughput read ≥ ceiling, physically impossible → endWall worker cutoff, the curve bends honestly (+regression); Timed trace replay took latency after SleepUntil instead of from scheduled time — the exact CO lie the harness exists to prevent → record-level observation from j.sched (+OpLag); VerifyPayload length-blind (a torn 4KiB-of-462KiB read passed as byte-identical) → VerifyPayloadLen everywhere; redis-py bar silently vanished from Chart 1 (driver emitted mode:asap, chart filtered mode:closed) + fill_bytes never sliced to n (oversized payloads) → --getbench closed mode + exact-size fill — the ≥10× comparison now rests on matched bytes. MEDs fixed: chart1 fabricated a %-ceiling annotation with no measured ceiling (now conditions-from-JSONL only); oplog parity precondition unforced (--oplog now requires --streams 1); GitSHA provenance empty (vcs.revision via ReadBuildInfo); zipf draw could return n (clamped); DaemonProbe scrape crash on short lines; run-chart1.sh port collision (6379 vs 9440) + max-not-median verdict + silent cell swallow; converter negative-timestamp + chain-length guards; loopback.sh kill-guard + blake3-free python parity; fetch.sh honest pinning. LOWs: writeHgrm hardcoded OpGet (a pure-PUT cell emitted an empty GET hgrm) → op follows cell mix; aws-gpu README overclaim → explicit runbook-not-scripts status; BENCHMARKS 50GbE misquote; comment/doc hygiene. | GATES post-fix: go build/vet clean repo-wide; lint 0 across bench/kvbench + metrics; -race green all 6 kvbench pkgs + metrics; loopback.sh acceptance ALL GREEN end-to-end (sweep ×2 + repeat verdict, injected-flip caught nonzero, converter count-exact, replay hit_rate=OUTPUT, Go↔Python op-sequence parity identical); both charts render from committed JSONL alone in a venv (Chart-2 crossover detected at 54%). C-31 review-debt: none carried. Merge Rule: Rudray owns coordinated omission (why latency counts from SCHEDULED send), the two-client rule (go-redis zero-copy vs redis-py — why both bars exist), hit-rate-as-OUTPUT in adaptive replay, and the 12 honesty rules before the charts circulate. |
| 2026-07-18 | Release engineering + launch docs: version stamping, goreleaser v2 static matrix, tag-driven release workflow, assert_static gate, deploy/{Dockerfile,kvblockd.service}, install.sh, quickstart configs, README rebuild, deployment-guide, INTEGRATIONS roadmap, CHANGELOG (staged commits) | LIGHT (blind: SDE-1 style/docs + SDE-2 correctness, both presented the diff as an external contribution; CTO evidence gate) — no concurrency/unsafe, docs+scripts+CI plumbing | SDE-1: 2H/2M/7L with explicit clean-sweep of links, claims-vs-evidence (12.67 GB/s, 100-loop torture, ~11 MB size all source-matched), CLI-grammar consistency, goreleaser/workflow coherence. SDE-2: 1 BLOCKER/2H/4M/2L, all CONFIRMED-or-verified, incl. live reproduction of the systemd crash-loop from a foreign cwd and a stale-evidence catch (the snapshot install-test predated the demo-tenant config — my smoke had not exercised the shipped chain). Refute-or-promote satisfied by cross-stage agreement on both HIGHs + SDE-2's end-to-end reproductions. | BLOCKER fixed: ghcr namespace — GITHUB_TOKEN can only push packages under the repo's own owner, so ghcr.io/kvstash/* dies with `denied` at the real tag run → image templates now use envOrDefault GHCR_OWNER, release.yml sets it from github.repository_owner (kvstash default = the post-org-transfer canonical). HIGHs fixed: (1) documented systemd install crash-looped — /etc copy of example.yaml kept a cwd-relative namespaces_path that systemd (cwd=/) can never resolve → unit header now copies namespaces.yaml + seds the path absolute; (2) curl|sh quickstart referenced config files that died with the installer's tmpdir → install.sh now installs configs to /usr/local/etc/kvblockd with namespaces_path rewritten absolute (no-clobber on real tenant files), README/epilogue updated to match. MEDs fixed: stale snapshot evidence → fresh snapshot + install-test + smoke re-run post-fix; container metrics bind dead through -p (127.0.0.1 inside the container) → documented KVBLOCKD_METRICS_ADDR=0.0.0.0:9442 + namespaces env in Dockerfile/guide; goreleaser version floated over a deprecated dockers config → pinned ~> v2.17 with a bump-deliberately note; /releases/latest 404s while only rcs exist → /releases fallback in install.sh. LOWs fixed: Rosetta arch detect (proc_translated), scratch-boot gate now HARD in CI (fails if docker is broken instead of passing vacuously), kvbctl usage gained the version line, setup-go aligned v7, CHANGELOG package path, guide sysctl/device-numbering copy-paste hygiene, INTEGRATIONS reserved-path notes, stale ~15MB comment. | Evidence gates (all post-fix, fresh artifacts): goreleaser check valid; snapshot release green end-to-end (6 static binaries, assert_static PASS ×6 incl. scratch boots, archives+SBOM+checksums+2 images); install-test via tarball on darwin AND ubuntu container; quickstart put/get/stats smoke green against the installed binaries + shipped configs; build/vet/lint 0 across ./...; -race -short full repo green; CI green on push; v0.1.0-rc1 tag → release.yml watched. Merge Rule (C-31): Rudray owns the three release explain-backs — GC-vs-100GB arena story, why CGO_ENABLED=0 makes a truly static binary (and what breaks it), and the full tag→stranger pipeline walk — plus the SPEC-7 hand-built release matrix drill before v0.1.0 final. |
| 2026-07-19 | AdmitMinHits zero-vs-default fix + admit-refusals counter + getbench throttle + rig hardening (4 commits) | FULL Ladder v2 (blind: SDE-1/2/3 + SSE on Fable 5 + Opus-4.8 breaker read-only; refute-or-promote via cross-stage agreement + TWO independent empirical reproductions of the test-bypass HIGH + a live PyPI check refuting my own lockfile claim) | per-stage yield: SDE-1 1B/1H/2M/6L (VERIFIED the pin claim false against live PyPI; empirically proved the regression test passes with the clamp restored), SDE-2 1H/4L (independently re-proved the test bypass with its own scratch-clamp build; audited all four Params constructors safe; confirmed the counter placement + JSON chain exact), SDE-3 0H/2L/1 INFO (traced the delete/evict race benign end-to-end: shard-locked DeleteIf, refcount-gated free, rel() exactly-once both branches; Append non-blocking; reaper margins safe), SSE 2H/4M/3L (config-layer contract gap; missing config round-trip + scrape-test coverage — the same silent-zero class the diff kills), breaker 1H/3M/2L (iperf artifact vs 100GbE headline contradiction; demote_drops help drift; fixture default flip; VERIFIED the production zero-path safe and the test mutation genuinely observed) | ALL fixed: pin truthed to 8.0.1 (requires-Python >=3.10 — AL2023 py3.9 pip FILTERS it from `pip index`; my "does not exist on PyPI" note was that filter, not reality; rig runs the driver on python3.11; driver JSONL now stamps redis_py+python provenance); regression tests rebuilt through the CONSTRUCTOR path + a direct withDefaults assertion, re-proven FAIL-with-clamp/ok-without; fixture default pinned to production (1) with an explicit newFixtureAdmit seam; config.go/example.yaml/DESIGN/deployment-guide all state the deletion-at-demote-watermark contract + the counter; config round-trip test (explicit 0 survives, absent stays 1) + scrape-line test added; iperf-ceiling.txt gained a rig+date provenance header (+ the script emits it) resolving the 49.8-vs-99.8 ambiguity; demote_drops help → publish-gate wording; MiB/s labels; probe message disambiguates admit-refusal vs eviction-overflow with remedies; run-chart1 timeout now kills the detached sweep, rc-read retries, per-store jsonl/cell.log reset; stray binaries gitignored. FOLLOW-UPS (Week 9 backlog): CompleteDemotion refused-vs-gone outcome enum (kills a transient policy-phantom re-Admit), demote_drops split by cause, modeltest coverage of the refuse path beyond the unit test. | Gates: build/vet clean; lint 0 across ./...; -race green (store, config, metrics, kvbench, full -short sweep); fail-with-clamp property re-verified post-rebuild; Docker/Linux end-to-end 973/973 demotions post-fix; i7i tier session green on the fixed daemon (T1a 5.22 / T1b 10.57 / T3 10.58 GB/s, T2 50-loop 0/0, $0 residue). Merge Rule: Rudray owns the zero-vs-default trap pattern (why withDefaults clamps are landmines and where defaults belong), the endurance-gate contract (what "demote at 90%" can DELETE and how the counter proves it), and why a regression test must travel the constructor path it protects. |
| 2026-07-19 | Rig T Chart-1 matrix + merged Rig N tier session (rig runs + results + docs) | RIG-RUN (proven scripts, hardened mid-session) + dead-man + $0-residue asserts both rigs | iperf3 ceiling 49.8 Gbit/s measured FIRST (provenance-labeled artifact); kvblockd full 7-point closed-GET curve ×3 runs; baselines median-of-3 at streams {8,32,64} (disclosed trim — single-threaded redis ≈4 min/cell); redis-py 8.0.1 on python3.11 with per-line provenance; tier session on the FIXED demotion path | CHART 1: kvblockd 6.22/6.23 GB/s = 100% of the 50 GbE ceiling both blob sizes (verify ON); valkey 2.38/1.83; redis 2.26/1.81; redis-py 0.88/0.83. GATE A-10x: 7.1x best-comparable / 5.6x median — BELOW 10x on this link, reported honestly: kvblockd is CEILING-LIMITED (100% of wire) while redis-py uses 14% of it; the c7gn 100 GbE session (12.67 GB/s) implies ~14x — the multiple is a property of the link; full 100 GbE matrix re-run = the certification path. TIER SESSION: fio ceilings 4.48 read / 3.65 write GB/s/device; T1a 5.22 GB/s one volume; T1b 10.57 both (2.0x scaling); T3 post-kill-9 warm storm 10.58 (zero recovery cost); T2 50-loop torture on real O_DIRECT/XFS: 0 corrupt, 0 phantom, 1,500+ byte-identical survivors. Session battle scars fed back into the scripts (compose/driver cwd bugs, detached-cell pattern, per-session ceiling provenance) — 3 aborted attempts cost ~$19 idle, all capped by dead-men. | Chart 1 rendered from committed JSONL alone (bench/results/rig-t/chart1.png); teardowns verified $0 both rigs; docs/BENCHMARKS.md Chart-1 section filled with the honest verdict; total rig spend this session ≈ $38. |
| 2026-07-20 | FULL v2 | W9 diff (~30 commits: tenancy quotas, S3 cold tier, obs, rigs, docs) | blind, 4×Fable-5 lenses + Opus read-only breaker + refuters | BLOCKER: 2 (tenant lock-graph deadlock REPRODUCED 2.2s; multi-volume S3 key collision) · HIGH: 4 (quota-limit races ×3-confirmed; publish→Transfer gap REPRODUCED leak+panic; spill-ack/flip zombie window; wire STATS pinned_bytes tenant leak) · MED: 11 · LOW: 9 · evidence-layer: artifact-dump BLOCKER + 2 docs-honesty HIGHs caught pre-push | per-stage yield: SDE-1 1B/2H, SDE-2 2H/3M, SDE-3 2B/3H (both repros), SSE 1H/1M, breaker 2 promoted (flip byte-mismatch, pin_quota bound) | all fixed in 9ed9d45 w/ reviewer repros ported as regressions; full gate green |
| 2026-07-22 | FULL v2 | S3 lazy whole-segment restore + cold-object GC (internal/store; uncommitted → v0.5.0) | blind: 2 construction passes → correctness + systems Fable-5 lenses (fresh context, diff presented as an external contribution) + refute-or-promote | construction ladder REPRODUCED a data-loss BLOCKER (restore adopted spilled=false + inline-dropped the object → restored blocks lost on EVERY tier when a reclaim landed before the next spill-ack); root fix = adopt spilled=true + retain object so reclaim flips not deletes. Both blind lenses then CONFIRMED the fix correct via mutation testing (M1/M2/M3/M5 all killed by the new tests) and CONVERGED on one real MED: the new reclaim latch head-of-line-blocks a volume's reclaim (FIFO break). · correctness lens: 0B/0H/2M/2L (found M4 coverage hole — the reclaim latch's REMOVAL survived every test) · systems lens: 0B/0H/2M/3L (melee + stop-cuts-restore + 15× leak probes; no deadlock/leak/crash-window/nominal-unbounded-growth) | MED reclaim-HOL FIXED (skip-to-next-oldest via a per-pass floor = skip-set on monotonic ids; also un-wedges a leaked latch); M4 hole CLOSED with a mid-walk seam test (red-proofed: FAILS with the latch removed, PASSES with it); 5×LOW + GC orphan channels + reclaim-latch availability trade LEDGERED in IMPROVEMENTS.md. Gates (independently re-run): 3× fresh `-race -count=1` + `-tags kvbdebug` + `GOMAXPROCS=1 -race` green, all 5 restore tests green, lint 0. Merge Rule: Rudray owns (1) why spilled=true+retain-object is the whole fix, (2) the reclaim floor=skip-set argument, (3) the spillInflight try-acquire discipline, (4) the s3SegRefs +1/−1 pairing. | PASS — merged, v0.5.0 (local tag) |
