# Day 90: what shipped, what didn't (retro)

<!-- SKELETON ONLY — Rudray authors the prose. This is a building-in-public
     retro and must be his voice; no section below publishes until he has
     written it himself. House rules for the prose: every number is
     config-qualified (rig, config, repro path — see bench/METHODOLOGY.md);
     zero unreproducible claims; misses stated as plainly as hits. -->

_Draft status: SKELETON — not published._

## What shipped

<!-- Prompt (one paragraph each, your words): the single static binary and
     the three tiers; tenancy+quotas; the five integration paths and which
     ones carry DEFER/beta caveats; the release pipeline; the numbers that
     held — quote each with its rig and %-of-ceiling, never a bare GB/s. -->

## The hardest bug

<!-- Prompt: pick ONE war story and tell it end to end — symptom, the wrong
     first theory, what the evidence actually said, the fix, and the
     regression test that pins it. Say what it taught you about the system. -->

## The honest misses

<!-- Prompt: what was targeted and NOT hit, with the measured number next to
     the target — no softening. Include the gates still open and why
     (hardware ceiling, GPU budget, upstream churn), and anything you'd call
     a self-inflicted delay. -->

## What's next

<!-- Prompt: the v1.1 direction in your own words — GPU-validated
     integrations when funded, lazy restore + object GC, TLS/mTLS,
     the io_uring spike, the slim build — and the non-goals that are NOT
     changing (TCP-only, opaque blocks, free-forever tech). Link
     docs/ROADMAP.md rather than re-promising. -->
