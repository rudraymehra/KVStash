version: "2"
linters:
  default: standard
  enable:
    - gosec
    - bodyclose
    - errorlint
    - exhaustive
    - misspell
    - prealloc
    - unconvert
    - unparam
    - wastedassign
    - gocritic
  settings:
    gocritic:
      enable-all: false
      enabled-checks:
        - ruleguard
      settings:
        ruleguard:
          rules: ${base-path}/rules.go
          failOn: all
    gosec:
      excludes:
        - G103 # unsafe is load-bearing in the dram arena — reviewed per-PR instead
    staticcheck:
      checks: ["all"]
  exclusions:
    presets:
      - std-error-handling
formatters:
  enable:
    - gofumpt
    - goimports
